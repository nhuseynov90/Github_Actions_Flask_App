name: github_actios_flaskapp

on:
  push:
    branches: 
      - main

jobs: 
  test: 
    runs-on: ubuntu-latest 

    steps: 
    - name: Print hello message
      run: echo "Aloha from test"
  
  deploy: 
    runs-on: ubuntu-latest

    steps: 
    - name: Print Hello message
      run: echo "Aloha from deploy"
